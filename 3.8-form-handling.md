# 3.8 Form Handling

> **Mục tiêu**: Xây dựng form với validation, error messages, và UX tốt trong React Native — sử dụng React Hook Form + Zod.

---

## 📖 Tổng Quan

| Approach              | Pros                         | Cons                            |
| --------------------- | ---------------------------- | ------------------------------- |
| **useState** (manual) | Đơn giản, không cần lib      | Boilerplate nhiều, khó validate |
| **React Hook Form**   | Ít re-render, validation tốt | Cần cài thêm                    |
| **Formik**            | Phổ biến, docs tốt           | Re-render nhiều hơn RHF         |

**Khuyến nghị**: **React Hook Form + Zod** — ít re-render, type-safe validation.

---

## 1. Form Cơ Bản (useState)

```tsx
import React, { useState } from "react";
import {
  View,
  Text,
  TextInput,
  TouchableOpacity,
  StyleSheet,
  Alert,
} from "react-native";

interface FormData {
  name: string;
  email: string;
  password: string;
}

interface FormErrors {
  name?: string;
  email?: string;
  password?: string;
}

const BasicForm = () => {
  const [form, setForm] = useState<FormData>({
    name: "",
    email: "",
    password: "",
  });
  const [errors, setErrors] = useState<FormErrors>({});

  // Validate manually
  const validate = (): boolean => {
    const newErrors: FormErrors = {};

    if (!form.name.trim()) newErrors.name = "Tên không được để trống";
    if (!form.email.trim()) newErrors.email = "Email không được để trống";
    else if (!/\S+@\S+\.\S+/.test(form.email))
      newErrors.email = "Email không hợp lệ";
    if (form.password.length < 6)
      newErrors.password = "Mật khẩu tối thiểu 6 ký tự";

    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = () => {
    if (validate()) {
      Alert.alert("Success", JSON.stringify(form, null, 2));
    }
  };

  const updateField = (field: keyof FormData, value: string) => {
    setForm((prev) => ({ ...prev, [field]: value }));
    // Clear error khi user bắt đầu sửa
    if (errors[field]) {
      setErrors((prev) => ({ ...prev, [field]: undefined }));
    }
  };

  return (
    <View style={styles.container}>
      <Text style={styles.label}>Tên</Text>
      <TextInput
        style={[styles.input, errors.name && styles.inputError]}
        value={form.name}
        onChangeText={(v) => updateField("name", v)}
        placeholder="Nhập tên"
      />
      {errors.name && <Text style={styles.error}>{errors.name}</Text>}

      <Text style={styles.label}>Email</Text>
      <TextInput
        style={[styles.input, errors.email && styles.inputError]}
        value={form.email}
        onChangeText={(v) => updateField("email", v)}
        placeholder="email@example.com"
        keyboardType="email-address"
        autoCapitalize="none"
      />
      {errors.email && <Text style={styles.error}>{errors.email}</Text>}

      <Text style={styles.label}>Mật khẩu</Text>
      <TextInput
        style={[styles.input, errors.password && styles.inputError]}
        value={form.password}
        onChangeText={(v) => updateField("password", v)}
        placeholder="Tối thiểu 6 ký tự"
        secureTextEntry
      />
      {errors.password && <Text style={styles.error}>{errors.password}</Text>}

      <TouchableOpacity style={styles.btn} onPress={handleSubmit}>
        <Text style={styles.btnText}>Đăng Ký</Text>
      </TouchableOpacity>
    </View>
  );
};

const styles = StyleSheet.create({
  container: { padding: 20 },
  label: {
    fontSize: 14,
    fontWeight: "600",
    color: "#333",
    marginTop: 16,
    marginBottom: 6,
  },
  input: {
    borderWidth: 1,
    borderColor: "#ddd",
    borderRadius: 8,
    padding: 12,
    fontSize: 16,
    backgroundColor: "#fafafa",
  },
  inputError: { borderColor: "#dc3545", backgroundColor: "#fff5f5" },
  error: { color: "#dc3545", fontSize: 13, marginTop: 4 },
  btn: {
    backgroundColor: "#007AFF",
    padding: 16,
    borderRadius: 10,
    alignItems: "center",
    marginTop: 24,
  },
  btnText: { color: "#fff", fontSize: 16, fontWeight: "600" },
});

export default BasicForm;
```

---

## 2. React Hook Form + Zod

### 2.1 Cài Đặt

```bash
npx expo install react-hook-form zod @hookform/resolvers
```

### 2.2 Schema Validation với Zod

```tsx
// schemas/auth.ts
import { z } from "zod";

export const loginSchema = z.object({
  email: z
    .string()
    .min(1, "Email không được để trống")
    .email("Email không hợp lệ"),
  password: z
    .string()
    .min(6, "Mật khẩu tối thiểu 6 ký tự")
    .max(50, "Mật khẩu tối đa 50 ký tự"),
});

export const registerSchema = z
  .object({
    name: z
      .string()
      .min(2, "Tên tối thiểu 2 ký tự")
      .max(50, "Tên tối đa 50 ký tự"),
    email: z
      .string()
      .min(1, "Email không được để trống")
      .email("Email không hợp lệ"),
    phone: z
      .string()
      .regex(/^(0[3|5|7|8|9])\d{8}$/, "Số điện thoại không hợp lệ")
      .optional()
      .or(z.literal("")),
    password: z
      .string()
      .min(8, "Mật khẩu tối thiểu 8 ký tự")
      .regex(/[A-Z]/, "Phải có ít nhất 1 chữ hoa")
      .regex(/[0-9]/, "Phải có ít nhất 1 số")
      .regex(/[^A-Za-z0-9]/, "Phải có ít nhất 1 ký tự đặc biệt"),
    confirmPassword: z.string(),
  })
  .refine((data) => data.password === data.confirmPassword, {
    message: "Mật khẩu xác nhận không khớp",
    path: ["confirmPassword"],
  });

// Infer TypeScript types từ schema!
export type LoginFormData = z.infer<typeof loginSchema>;
export type RegisterFormData = z.infer<typeof registerSchema>;
```

### 2.3 Login Form

```tsx
// screens/LoginScreen.tsx
import React, { useState } from "react";
import {
  View,
  Text,
  TextInput,
  TouchableOpacity,
  StyleSheet,
  ActivityIndicator,
  KeyboardAvoidingView,
  Platform,
  ScrollView,
} from "react-native";
import { useForm, Controller } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { loginSchema, LoginFormData } from "../schemas/auth";

const LoginScreen = () => {
  const [isSubmitting, setIsSubmitting] = useState(false);

  const {
    control,
    handleSubmit,
    formState: { errors, isValid },
    reset,
  } = useForm<LoginFormData>({
    resolver: zodResolver(loginSchema),
    defaultValues: { email: "", password: "" },
    mode: "onBlur", // Validate khi blur (rời khỏi field)
  });

  const onSubmit = async (data: LoginFormData) => {
    setIsSubmitting(true);
    try {
      console.log("Login:", data);
      // await authService.login(data.email, data.password);
      await new Promise((r) => setTimeout(r, 1500)); // Giả lập
      reset();
    } catch (error) {
      console.error(error);
    } finally {
      setIsSubmitting(false);
    }
  };

  return (
    <KeyboardAvoidingView
      style={{ flex: 1 }}
      behavior={Platform.OS === "ios" ? "padding" : "height"}
    >
      <ScrollView
        contentContainerStyle={lStyles.container}
        keyboardShouldPersistTaps="handled"
      >
        <Text style={lStyles.title}>Đăng Nhập</Text>

        {/* Email */}
        <Text style={lStyles.label}>Email</Text>
        <Controller
          control={control}
          name="email"
          render={({ field: { onChange, onBlur, value } }) => (
            <TextInput
              style={[lStyles.input, errors.email && lStyles.inputError]}
              onBlur={onBlur}
              onChangeText={onChange}
              value={value}
              placeholder="email@example.com"
              keyboardType="email-address"
              autoCapitalize="none"
              autoCorrect={false}
            />
          )}
        />
        {errors.email && (
          <Text style={lStyles.error}>{errors.email.message}</Text>
        )}

        {/* Password */}
        <Text style={lStyles.label}>Mật khẩu</Text>
        <Controller
          control={control}
          name="password"
          render={({ field: { onChange, onBlur, value } }) => (
            <TextInput
              style={[lStyles.input, errors.password && lStyles.inputError]}
              onBlur={onBlur}
              onChangeText={onChange}
              value={value}
              placeholder="Tối thiểu 6 ký tự"
              secureTextEntry
            />
          )}
        />
        {errors.password && (
          <Text style={lStyles.error}>{errors.password.message}</Text>
        )}

        {/* Submit */}
        <TouchableOpacity
          style={[
            lStyles.btn,
            (!isValid || isSubmitting) && lStyles.btnDisabled,
          ]}
          onPress={handleSubmit(onSubmit)}
          disabled={isSubmitting}
        >
          {isSubmitting ? (
            <ActivityIndicator color="#fff" />
          ) : (
            <Text style={lStyles.btnText}>Đăng Nhập</Text>
          )}
        </TouchableOpacity>
      </ScrollView>
    </KeyboardAvoidingView>
  );
};

const lStyles = StyleSheet.create({
  container: { flexGrow: 1, padding: 24, justifyContent: "center" },
  title: {
    fontSize: 28,
    fontWeight: "bold",
    textAlign: "center",
    marginBottom: 32,
  },
  label: {
    fontSize: 14,
    fontWeight: "600",
    color: "#333",
    marginTop: 16,
    marginBottom: 6,
  },
  input: {
    borderWidth: 1,
    borderColor: "#ddd",
    borderRadius: 10,
    padding: 14,
    fontSize: 16,
    backgroundColor: "#fafafa",
  },
  inputError: { borderColor: "#dc3545", backgroundColor: "#fff5f5" },
  error: { color: "#dc3545", fontSize: 13, marginTop: 4 },
  btn: {
    backgroundColor: "#007AFF",
    padding: 16,
    borderRadius: 12,
    alignItems: "center",
    marginTop: 28,
  },
  btnDisabled: { opacity: 0.6 },
  btnText: { color: "#fff", fontSize: 17, fontWeight: "600" },
});

export default LoginScreen;
```

### 2.4 Reusable FormInput Component

```tsx
// components/FormInput.tsx
import React from "react";
import {
  View,
  Text,
  TextInput,
  TextInputProps,
  StyleSheet,
} from "react-native";
import { Control, Controller, FieldValues, Path } from "react-hook-form";

interface FormInputProps<T extends FieldValues> extends Omit<
  TextInputProps,
  "value"
> {
  control: Control<T>;
  name: Path<T>;
  label: string;
  error?: string;
}

export function FormInput<T extends FieldValues>({
  control,
  name,
  label,
  error,
  ...inputProps
}: FormInputProps<T>) {
  return (
    <View style={fiStyles.field}>
      <Text style={fiStyles.label}>{label}</Text>
      <Controller
        control={control}
        name={name}
        render={({ field: { onChange, onBlur, value } }) => (
          <TextInput
            style={[fiStyles.input, error && fiStyles.inputError]}
            onBlur={onBlur}
            onChangeText={onChange}
            value={value}
            placeholderTextColor="#999"
            {...inputProps}
          />
        )}
      />
      {error && <Text style={fiStyles.error}>{error}</Text>}
    </View>
  );
}

const fiStyles = StyleSheet.create({
  field: { marginBottom: 16 },
  label: { fontSize: 14, fontWeight: "600", color: "#333", marginBottom: 6 },
  input: {
    borderWidth: 1,
    borderColor: "#ddd",
    borderRadius: 10,
    padding: 14,
    fontSize: 16,
    backgroundColor: "#fafafa",
  },
  inputError: { borderColor: "#dc3545", backgroundColor: "#fff5f5" },
  error: { color: "#dc3545", fontSize: 13, marginTop: 4 },
});
```

```tsx
// Sử dụng FormInput — code rất gọn!
import { FormInput } from "../components/FormInput";

const RegisterScreen = () => {
  const {
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<RegisterFormData>({
    resolver: zodResolver(registerSchema),
    mode: "onBlur",
  });

  return (
    <View style={{ padding: 24 }}>
      <FormInput
        control={control}
        name="name"
        label="Họ tên"
        error={errors.name?.message}
        placeholder="Nguyễn Văn A"
      />
      <FormInput
        control={control}
        name="email"
        label="Email"
        error={errors.email?.message}
        placeholder="email@example.com"
        keyboardType="email-address"
        autoCapitalize="none"
      />
      <FormInput
        control={control}
        name="phone"
        label="Số điện thoại"
        error={errors.phone?.message}
        placeholder="0912345678"
        keyboardType="phone-pad"
      />
      <FormInput
        control={control}
        name="password"
        label="Mật khẩu"
        error={errors.password?.message}
        placeholder="Mật khẩu mạnh"
        secureTextEntry
      />
      <FormInput
        control={control}
        name="confirmPassword"
        label="Xác nhận"
        error={errors.confirmPassword?.message}
        placeholder="Nhập lại mật khẩu"
        secureTextEntry
      />

      <TouchableOpacity style={lStyles.btn} onPress={handleSubmit(onSubmit)}>
        <Text style={lStyles.btnText}>Đăng Ký</Text>
      </TouchableOpacity>
    </View>
  );
};
```

---

## 3. TextInput Props Quan Trọng

```tsx
// Tất cả TextInput props hữu ích cho forms
<TextInput
  // Nội dung
  value={value}
  onChangeText={onChange}
  placeholder="Placeholder text"
  placeholderTextColor="#999"
  defaultValue="Default"
  // Keyboard
  keyboardType="default" // default | email-address | numeric | phone-pad | decimal-pad
  returnKeyType="next" // done | go | next | search | send
  autoCapitalize="none" // none | sentences | words | characters
  autoCorrect={false}
  autoComplete="email" // email | password | name | tel | off
  // Security
  secureTextEntry={true} // Password field
  textContentType="password" // iOS autofill: oneTimeCode, emailAddress, etc.
  // Behavior
  multiline={true} // Textarea
  numberOfLines={4}
  maxLength={100}
  editable={true}
  selectTextOnFocus={true}
  // Events
  onBlur={onBlur}
  onFocus={() => {}}
  onSubmitEditing={() => {}} // Khi nhấn Return/Done
  // Style
  style={styles.input}
/>
```

---

## 4. Keyboard Handling

```tsx
import React from "react";
import {
  View,
  TextInput,
  TouchableOpacity,
  Text,
  KeyboardAvoidingView,
  Platform,
  ScrollView,
  TouchableWithoutFeedback,
  Keyboard,
} from "react-native";

const KeyboardHandlingForm = () => (
  // ✅ KeyboardAvoidingView — đẩy content lên khi keyboard mở
  <KeyboardAvoidingView
    style={{ flex: 1 }}
    behavior={Platform.OS === "ios" ? "padding" : "height"}
    keyboardVerticalOffset={Platform.OS === "ios" ? 64 : 0} // Offset cho header
  >
    {/* ✅ TouchableWithoutFeedback — dismiss keyboard khi tap ngoài */}
    <TouchableWithoutFeedback onPress={Keyboard.dismiss}>
      <ScrollView
        contentContainerStyle={{ flexGrow: 1, padding: 20 }}
        // ✅ Không dismiss keyboard khi scroll/tap form elements
        keyboardShouldPersistTaps="handled"
      >
        <TextInput
          placeholder="Field 1"
          returnKeyType="next"
          onSubmitEditing={() => {
            // Focus field tiếp theo
            // field2Ref.current?.focus();
          }}
          blurOnSubmit={false} // Không dismiss keyboard khi nhấn Next
          style={{
            borderWidth: 1,
            borderColor: "#ddd",
            borderRadius: 8,
            padding: 12,
            marginBottom: 12,
          }}
        />
        <TextInput
          placeholder="Field 2"
          returnKeyType="done"
          onSubmitEditing={Keyboard.dismiss}
          style={{
            borderWidth: 1,
            borderColor: "#ddd",
            borderRadius: 8,
            padding: 12,
            marginBottom: 12,
          }}
        />
        <TouchableOpacity
          style={{
            backgroundColor: "#007AFF",
            padding: 16,
            borderRadius: 10,
            alignItems: "center",
          }}
        >
          <Text style={{ color: "#fff", fontWeight: "600" }}>Submit</Text>
        </TouchableOpacity>
      </ScrollView>
    </TouchableWithoutFeedback>
  </KeyboardAvoidingView>
);
```

---

## 5. Custom Components: Select, Switch, DatePicker

### 5.1 Custom Select (Picker)

```tsx
// components/FormSelect.tsx
import React, { useState } from "react";
import {
  View,
  Text,
  TouchableOpacity,
  Modal,
  FlatList,
  StyleSheet,
} from "react-native";

interface Option {
  label: string;
  value: string;
}

interface FormSelectProps {
  label: string;
  options: Option[];
  value: string;
  onChange: (value: string) => void;
  error?: string;
  placeholder?: string;
}

const FormSelect = ({
  label,
  options,
  value,
  onChange,
  error,
  placeholder = "Chọn...",
}: FormSelectProps) => {
  const [visible, setVisible] = useState(false);
  const selectedLabel = options.find((o) => o.value === value)?.label;

  return (
    <View style={selStyles.field}>
      <Text style={selStyles.label}>{label}</Text>
      <TouchableOpacity
        style={[selStyles.select, error && { borderColor: "#dc3545" }]}
        onPress={() => setVisible(true)}
      >
        <Text
          style={[selStyles.selectText, !selectedLabel && { color: "#999" }]}
        >
          {selectedLabel || placeholder}
        </Text>
        <Text>▼</Text>
      </TouchableOpacity>
      {error && <Text style={selStyles.error}>{error}</Text>}

      <Modal visible={visible} transparent animationType="slide">
        <TouchableOpacity
          style={selStyles.overlay}
          activeOpacity={1}
          onPress={() => setVisible(false)}
        >
          <View style={selStyles.sheet}>
            <Text style={selStyles.sheetTitle}>{label}</Text>
            <FlatList
              data={options}
              keyExtractor={(item) => item.value}
              renderItem={({ item }) => (
                <TouchableOpacity
                  style={[
                    selStyles.option,
                    item.value === value && selStyles.optionActive,
                  ]}
                  onPress={() => {
                    onChange(item.value);
                    setVisible(false);
                  }}
                >
                  <Text
                    style={[
                      selStyles.optionText,
                      item.value === value && {
                        color: "#007AFF",
                        fontWeight: "600",
                      },
                    ]}
                  >
                    {item.label}
                  </Text>
                  {item.value === value && <Text>✓</Text>}
                </TouchableOpacity>
              )}
            />
          </View>
        </TouchableOpacity>
      </Modal>
    </View>
  );
};

const selStyles = StyleSheet.create({
  field: { marginBottom: 16 },
  label: { fontSize: 14, fontWeight: "600", marginBottom: 6 },
  select: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    borderWidth: 1,
    borderColor: "#ddd",
    borderRadius: 10,
    padding: 14,
    backgroundColor: "#fafafa",
  },
  selectText: { fontSize: 16 },
  error: { color: "#dc3545", fontSize: 13, marginTop: 4 },
  overlay: {
    flex: 1,
    justifyContent: "flex-end",
    backgroundColor: "rgba(0,0,0,0.4)",
  },
  sheet: {
    backgroundColor: "#fff",
    borderTopLeftRadius: 20,
    borderTopRightRadius: 20,
    maxHeight: "50%",
    padding: 16,
  },
  sheetTitle: {
    fontSize: 18,
    fontWeight: "bold",
    textAlign: "center",
    paddingVertical: 12,
    borderBottomWidth: 1,
    borderBottomColor: "#f0f0f0",
  },
  option: {
    flexDirection: "row",
    justifyContent: "space-between",
    padding: 16,
    borderBottomWidth: 1,
    borderBottomColor: "#f5f5f5",
  },
  optionActive: { backgroundColor: "#f0f7ff" },
  optionText: { fontSize: 16 },
});

export default FormSelect;
```

### 5.2 Sử dụng Select với React Hook Form

```tsx
<Controller
  control={control}
  name="gender"
  render={({ field: { onChange, value } }) => (
    <FormSelect
      label="Giới tính"
      options={[
        { label: "Nam", value: "male" },
        { label: "Nữ", value: "female" },
        { label: "Khác", value: "other" },
      ]}
      value={value}
      onChange={onChange}
      error={errors.gender?.message}
    />
  )}
/>
```

---

## 🏋️ Bài Tập Thực Hành

### Bài 1: Login Form

Tạo form đăng nhập với React Hook Form + Zod:

- Email + Password validation
- Loading state khi submit
- Error display
- Keyboard handling

### Bài 2: Registration Form

Tạo form đăng ký đầy đủ:

- Name, Email, Phone, Password, Confirm Password
- Zod schema với password match
- Custom FormInput component
- Custom Select cho Gender

### Bài 3: Edit Profile Form

Tạo form chỉnh sửa profile:

- Pre-fill data từ API
- Avatar picker (placeholder)
- Form dirty detection
- Save/Discard changes

---

## 📚 Tài Liệu Tham Khảo

| Nguồn                     | Link                                              |
| ------------------------- | ------------------------------------------------- |
| React Hook Form           | https://react-hook-form.com/                      |
| Zod                       | https://zod.dev/                                  |
| @hookform/resolvers       | https://github.com/react-hook-form/resolvers      |
| RN — TextInput            | https://reactnative.dev/docs/textinput            |
| RN — KeyboardAvoidingView | https://reactnative.dev/docs/keyboardavoidingview |

---

## ✅ Checklist Tự Đánh Giá

### Cơ bản

- [ ] Form với useState
- [ ] Manual validation
- [ ] Error display

### React Hook Form

- [ ] `useForm` + `Controller`
- [ ] Zod schema + `zodResolver`
- [ ] Validation modes: onBlur, onChange, onSubmit
- [ ] `handleSubmit`, `formState.errors`
- [ ] Reusable FormInput component
- [ ] `reset()`, `setValue()`, `watch()`

### UX

- [ ] `KeyboardAvoidingView`
- [ ] `keyboardShouldPersistTaps`
- [ ] Focus next field (`returnKeyType: 'next'`)
- [ ] Dismiss keyboard
- [ ] Loading state khi submit
- [ ] Disabled button khi invalid

### Custom Components

- [ ] Select/Picker (Modal bottom sheet)
- [ ] Switch/Toggle
- [ ] Integration với React Hook Form Controller

> **Trước đó**: [← 3.7 State Management](./3.7-state-management.md) | **Tiếp theo**: [3.9 Authentication Flow →](./3.9-authentication-flow.md)

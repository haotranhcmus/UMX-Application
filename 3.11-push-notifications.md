# 3.11 Push Notifications

> **Mục tiêu**: Implement push notifications trong React Native với Expo — local notifications, remote push (FCM/APNs), và handling notification events.

---

## 📖 Tổng Quan

### Loại Notifications

| Loại              | Mô tả                              | Use case                                |
| ----------------- | ---------------------------------- | --------------------------------------- |
| **Local**         | App tự trigger, không cần server   | Reminders, alarms, timers               |
| **Remote (Push)** | Server gửi qua FCM/APNs            | Chat messages, order updates, marketing |
| **Scheduled**     | Đặt lịch hiện tại thời điểm cụ thể | Daily reminders, scheduled events       |

### Architecture

```
┌─── Remote Push ──────────────────────────────────┐
│                                                    │
│  Your Server → Expo Push Service → FCM/APNs → App │
│                                                    │
│  1. App đăng ký → nhận Expo Push Token             │
│  2. Gửi token lên server                           │
│  3. Server gửi notification qua Expo Push API      │
│  4. Expo chuyển qua FCM (Android) / APNs (iOS)    │
│  5. Device nhận và hiển thị notification           │
│                                                    │
└────────────────────────────────────────────────────┘
```

---

## 1. Cài Đặt

```bash
npx expo install expo-notifications expo-device expo-constants
```

**app.json** — config (tự động nếu dùng Expo):

```json
{
  "expo": {
    "plugins": [
      [
        "expo-notifications",
        {
          "icon": "./assets/notification-icon.png",
          "color": "#ffffff",
          "sounds": ["./assets/notification-sound.wav"]
        }
      ]
    ],
    "android": {
      "useNextNotificationsApi": true
    }
  }
}
```

---

## 2. Setup & Đăng Ký Push Token

```tsx
// services/notifications.ts
import * as Notifications from "expo-notifications";
import * as Device from "expo-device";
import Constants from "expo-constants";
import { Platform } from "react-native";

// ===== Cấu hình cách hiển thị notification khi app đang mở =====
Notifications.setNotificationHandler({
  handleNotification: async () => ({
    shouldShowAlert: true, // Hiển thị alert
    shouldPlaySound: true, // Phát âm thanh
    shouldSetBadge: true, // Cập nhật badge count
  }),
});

// ===== Đăng ký nhận Push Notifications =====
export async function registerForPushNotifications(): Promise<string | null> {
  // Chỉ hoạt động trên device thật
  if (!Device.isDevice) {
    console.warn("Push notifications only work on physical devices");
    return null;
  }

  // Check & request permission
  const { status: existingStatus } = await Notifications.getPermissionsAsync();
  let finalStatus = existingStatus;

  if (existingStatus !== "granted") {
    const { status } = await Notifications.requestPermissionsAsync();
    finalStatus = status;
  }

  if (finalStatus !== "granted") {
    console.warn("Push notification permission denied");
    return null;
  }

  // Lấy Expo Push Token
  try {
    const projectId = Constants.expoConfig?.extra?.eas?.projectId;
    const tokenData = await Notifications.getExpoPushTokenAsync({
      projectId,
    });

    console.log("Expo Push Token:", tokenData.data);

    // Android: cần tạo notification channel
    if (Platform.OS === "android") {
      await Notifications.setNotificationChannelAsync("default", {
        name: "Default",
        importance: Notifications.AndroidImportance.MAX,
        vibrationPattern: [0, 250, 250, 250],
        lightColor: "#FF231F7C",
        sound: "default",
      });
    }

    return tokenData.data; // "ExponentPushToken[xxxxxxxxxxxxxxxxxxxxxx]"
  } catch (error) {
    console.error("Error getting push token:", error);
    return null;
  }
}

// ===== Gửi token lên server =====
export async function sendTokenToServer(
  token: string,
  userId: string,
): Promise<void> {
  try {
    await fetch("https://your-api.com/api/push-tokens", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ token, userId, platform: Platform.OS }),
    });
  } catch (error) {
    console.error("Failed to send token to server:", error);
  }
}
```

---

## 3. Local Notifications

### 3.1 Gửi Ngay Lập Tức

```tsx
import * as Notifications from "expo-notifications";

// Gửi local notification ngay
export async function sendLocalNotification(
  title: string,
  body: string,
  data?: Record<string, any>,
): Promise<string> {
  const id = await Notifications.scheduleNotificationAsync({
    content: {
      title,
      body,
      data: data || {},
      sound: "default",
      // Android specific
      priority: Notifications.AndroidNotificationPriority.HIGH,
      // iOS specific
      badge: 1,
    },
    trigger: null, // null = gửi ngay lập tức
  });

  return id;
}

// Sử dụng:
// await sendLocalNotification('Hello!', 'This is a test notification', { screen: 'Detail', id: 123 });
```

### 3.2 Scheduled Notifications

```tsx
// Gửi sau N giây
export async function scheduleNotification(
  title: string,
  body: string,
  seconds: number,
  data?: Record<string, any>,
): Promise<string> {
  const id = await Notifications.scheduleNotificationAsync({
    content: { title, body, data: data || {}, sound: "default" },
    trigger: {
      type: Notifications.SchedulableTriggerInputTypes.TIME_INTERVAL,
      seconds,
      repeats: false,
    },
  });
  return id;
}

// Gửi vào thời điểm cụ thể
export async function scheduleAtDate(
  title: string,
  body: string,
  date: Date,
  data?: Record<string, any>,
): Promise<string> {
  const id = await Notifications.scheduleNotificationAsync({
    content: { title, body, data: data || {}, sound: "default" },
    trigger: {
      type: Notifications.SchedulableTriggerInputTypes.DATE,
      date,
    },
  });
  return id;
}

// Gửi lặp lại hàng ngày (ví dụ: 8:00 AM)
export async function scheduleDailyReminder(
  title: string,
  body: string,
  hour: number,
  minute: number,
): Promise<string> {
  const id = await Notifications.scheduleNotificationAsync({
    content: { title, body, sound: "default" },
    trigger: {
      type: Notifications.SchedulableTriggerInputTypes.CALENDAR,
      hour,
      minute,
      repeats: true,
    },
  });
  return id;
}

// Hủy notification đã schedule
export async function cancelNotification(id: string): Promise<void> {
  await Notifications.cancelScheduledNotificationAsync(id);
}

// Hủy tất cả
export async function cancelAllNotifications(): Promise<void> {
  await Notifications.cancelAllScheduledNotificationsAsync();
}

// Xem các notifications đã schedule
export async function getScheduledNotifications() {
  return Notifications.getAllScheduledNotificationsAsync();
}
```

---

## 4. Handling Notification Events

### 4.1 Hook — useNotifications

```tsx
// hooks/useNotifications.ts
import { useState, useEffect, useRef } from "react";
import * as Notifications from "expo-notifications";
import { registerForPushNotifications } from "../services/notifications";

interface UseNotificationsResult {
  expoPushToken: string | null;
  notification: Notifications.Notification | null;
  error: string | null;
}

export function useNotifications(): UseNotificationsResult {
  const [expoPushToken, setExpoPushToken] = useState<string | null>(null);
  const [notification, setNotification] =
    useState<Notifications.Notification | null>(null);
  const [error, setError] = useState<string | null>(null);

  const notificationListener = useRef<Notifications.EventSubscription>();
  const responseListener = useRef<Notifications.EventSubscription>();

  useEffect(() => {
    // 1. Đăng ký push token
    registerForPushNotifications()
      .then((token) => setExpoPushToken(token))
      .catch((err) => setError(err.message));

    // 2. Listener: notification đến khi app đang mở (foreground)
    notificationListener.current =
      Notifications.addNotificationReceivedListener((notification) => {
        console.log("📩 Notification received (foreground):", notification);
        setNotification(notification);
      });

    // 3. Listener: user tap vào notification
    responseListener.current =
      Notifications.addNotificationResponseReceivedListener((response) => {
        console.log("👆 Notification tapped:", response);
        const data = response.notification.request.content.data;

        // Navigate dựa trên data
        handleNotificationNavigation(data);
      });

    return () => {
      if (notificationListener.current) {
        Notifications.removeNotificationSubscription(
          notificationListener.current,
        );
      }
      if (responseListener.current) {
        Notifications.removeNotificationSubscription(responseListener.current);
      }
    };
  }, []);

  return { expoPushToken, notification, error };
}

// Navigate dựa trên notification data
function handleNotificationNavigation(data: Record<string, any>) {
  if (data.screen === "Detail" && data.id) {
    // navigation.navigate('Detail', { id: data.id });
    console.log(`Navigate to Detail screen with id: ${data.id}`);
  } else if (data.screen === "Chat") {
    // navigation.navigate('Chat', { conversationId: data.conversationId });
    console.log("Navigate to Chat screen");
  }
}
```

### 4.2 Sử Dụng trong App

```tsx
// App.tsx hoặc Root component
import React from "react";
import { View, Text, TouchableOpacity, StyleSheet, Alert } from "react-native";
import { useNotifications } from "./hooks/useNotifications";
import {
  sendLocalNotification,
  scheduleNotification,
} from "./services/notifications";

const NotificationDemo = () => {
  const { expoPushToken, notification } = useNotifications();

  return (
    <View style={styles.container}>
      {/* Token info */}
      <View style={styles.tokenBox}>
        <Text style={styles.label}>Push Token:</Text>
        <Text style={styles.token} selectable numberOfLines={2}>
          {expoPushToken || "Đang lấy token..."}
        </Text>
      </View>

      {/* Last notification */}
      {notification && (
        <View style={styles.notifBox}>
          <Text style={styles.label}>Last Notification:</Text>
          <Text style={styles.notifTitle}>
            {notification.request.content.title}
          </Text>
          <Text style={styles.notifBody}>
            {notification.request.content.body}
          </Text>
        </View>
      )}

      {/* Test buttons */}
      <View style={styles.buttons}>
        <TouchableOpacity
          style={styles.btn}
          onPress={() =>
            sendLocalNotification(
              "🔔 Test Notification",
              "This is a local notification!",
              { screen: "Detail", id: 42 },
            )
          }
        >
          <Text style={styles.btnText}>Send Now</Text>
        </TouchableOpacity>

        <TouchableOpacity
          style={[styles.btn, { backgroundColor: "#4ecdc4" }]}
          onPress={() =>
            scheduleNotification(
              "⏰ Scheduled",
              "This appeared after 5 seconds!",
              5,
            )
          }
        >
          <Text style={styles.btnText}>Schedule (5s)</Text>
        </TouchableOpacity>
      </View>
    </View>
  );
};

const styles = StyleSheet.create({
  container: { flex: 1, padding: 20, backgroundColor: "#fff" },
  tokenBox: {
    backgroundColor: "#f0f0f0",
    padding: 12,
    borderRadius: 8,
    marginBottom: 16,
  },
  label: { fontSize: 13, fontWeight: "600", color: "#666", marginBottom: 4 },
  token: { fontSize: 12, color: "#333", fontFamily: "monospace" },
  notifBox: {
    backgroundColor: "#e8f4fd",
    padding: 12,
    borderRadius: 8,
    marginBottom: 16,
    borderLeftWidth: 4,
    borderLeftColor: "#007AFF",
  },
  notifTitle: { fontSize: 16, fontWeight: "bold", marginTop: 4 },
  notifBody: { fontSize: 14, color: "#666", marginTop: 2 },
  buttons: { gap: 12 },
  btn: {
    backgroundColor: "#007AFF",
    padding: 16,
    borderRadius: 12,
    alignItems: "center",
  },
  btnText: { color: "#fff", fontSize: 16, fontWeight: "600" },
});

export default NotificationDemo;
```

---

## 5. Server-Side: Gửi Push Notification

### 5.1 Expo Push API (từ Server)

```tsx
// Backend (Node.js) — gửi push notification
// npm install expo-server-sdk

import { Expo, ExpoPushMessage } from "expo-server-sdk";

const expo = new Expo();

async function sendPushNotification(
  pushTokens: string[],
  title: string,
  body: string,
  data?: Record<string, any>,
) {
  const messages: ExpoPushMessage[] = pushTokens
    .filter((token) => Expo.isExpoPushToken(token))
    .map((token) => ({
      to: token,
      sound: "default",
      title,
      body,
      data: data || {},
      priority: "high",
    }));

  // Gửi theo batch (Expo giới hạn)
  const chunks = expo.chunkPushNotifications(messages);
  const tickets = [];

  for (const chunk of chunks) {
    try {
      const ticketChunk = await expo.sendPushNotificationsAsync(chunk);
      tickets.push(...ticketChunk);
    } catch (error) {
      console.error("Error sending push:", error);
    }
  }

  return tickets;
}

// Sử dụng:
// await sendPushNotification(
//   ['ExponentPushToken[xxx]'],
//   '📦 Order Update',
//   'Your order #123 has been shipped!',
//   { screen: 'OrderDetail', orderId: '123' }
// );
```

### 5.2 Test Push (cURL / Expo Tool)

```bash
# Test gửi push notification qua Expo API
curl -H "Content-Type: application/json" \
  -d '{
    "to": "ExponentPushToken[xxxxxxxxxxxxxxxxxxxxxx]",
    "title": "Test Push",
    "body": "Hello from server!",
    "data": { "screen": "Home" }
  }' \
  https://exp.host/--/api/v2/push/send
```

Hoặc dùng **Expo Push Notification Tool**: https://expo.dev/notifications

---

## 6. Badge Count

```tsx
import * as Notifications from "expo-notifications";

// Set badge count
await Notifications.setBadgeCountAsync(5);

// Get badge count
const count = await Notifications.getBadgeCountAsync();

// Clear badge
await Notifications.setBadgeCountAsync(0);
```

---

## 7. Notification Categories (Action Buttons)

```tsx
import * as Notifications from "expo-notifications";

// Định nghĩa categories với action buttons
await Notifications.setNotificationCategoryAsync("message", [
  {
    identifier: "reply",
    buttonTitle: "Reply",
    options: {
      opensAppToForeground: true,
    },
    textInput: {
      submitButtonTitle: "Send",
      placeholder: "Type a reply...",
    },
  },
  {
    identifier: "mark-read",
    buttonTitle: "Mark as Read",
    options: {
      opensAppToForeground: false,
    },
  },
]);

// Gửi notification với category
await Notifications.scheduleNotificationAsync({
  content: {
    title: "New Message",
    body: "Hey, how are you?",
    categoryIdentifier: "message", // ← Link to category
    data: { conversationId: "123" },
  },
  trigger: null,
});

// Handle action response
Notifications.addNotificationResponseReceivedListener((response) => {
  const actionId = response.actionIdentifier;
  const userText = response.userText; // Nếu có text input

  if (actionId === "reply") {
    console.log("User replied:", userText);
  } else if (actionId === "mark-read") {
    console.log("Marked as read");
  }
});
```

---

## 🏋️ Bài Tập Thực Hành

### Bài 1: Local Notification

Tạo app gửi local notification:

- Button "Send Now"
- Button "Schedule 10s"
- Hiển thị last notification info

### Bài 2: Daily Reminder

Tạo tính năng daily reminder:

- User chọn giờ (picker)
- Schedule notification hàng ngày
- Toggle on/off
- Lưu setting vào AsyncStorage

### Bài 3: Push Notification

- Đăng ký push token
- Gửi token lên server
- Handle notification tap → navigate đúng screen

---

## 📚 Tài Liệu Tham Khảo

| Nguồn              | Link                                                     |
| ------------------ | -------------------------------------------------------- |
| Expo Notifications | https://docs.expo.dev/versions/latest/sdk/notifications/ |
| Expo Push Guide    | https://docs.expo.dev/push-notifications/overview/       |
| Expo Push Tool     | https://expo.dev/notifications                           |
| expo-server-sdk    | https://github.com/expo/expo-server-sdk-node             |
| FCM (Firebase)     | https://firebase.google.com/docs/cloud-messaging         |

---

## ✅ Checklist Tự Đánh Giá

### Setup

- [ ] Cài expo-notifications, expo-device
- [ ] `setNotificationHandler` (foreground config)
- [ ] Android notification channel

### Push Token

- [ ] Request permission
- [ ] `getExpoPushTokenAsync()`
- [ ] Gửi token lên server

### Local Notifications

- [ ] `scheduleNotificationAsync` — trigger: null (ngay)
- [ ] Schedule sau N giây
- [ ] Schedule theo ngày/giờ (calendar)
- [ ] Repeating notifications
- [ ] Cancel scheduled notifications

### Event Handling

- [ ] `addNotificationReceivedListener` (foreground)
- [ ] `addNotificationResponseReceivedListener` (tap)
- [ ] Navigate dựa trên notification data
- [ ] Cleanup listeners

### Advanced

- [ ] Badge count
- [ ] Notification categories (action buttons)
- [ ] Server-side sending (Expo Push API)

> **Trước đó**: [← 3.10 Local Storage](./3.10-local-storage.md) | **Tiếp theo**: [3.12 Build & Release →](./3.12-build-release.md)

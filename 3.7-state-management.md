# 3.7 State Management

> **Mục tiêu**: Hiểu và áp dụng các pattern quản lý state trong React Native — từ cơ bản (useState, Context) đến nâng cao (Zustand, Redux Toolkit).

---

## 📖 Tổng Quan

### Khi nào cần State Management?

| Tình huống                 | Giải pháp                    |
| -------------------------- | ---------------------------- |
| State local 1 component    | `useState`                   |
| State chia sẻ parent-child | Props drilling               |
| State chia sẻ 2-3 cấp      | `useContext`                 |
| State chia sẻ toàn app     | Zustand / Redux Toolkit      |
| Server state (API data)    | TanStack Query (React Query) |
| Form state                 | React Hook Form              |

### So Sánh Thư Viện

|             | Context API | Zustand      | Redux Toolkit | React Query  |
| ----------- | ----------- | ------------ | ------------- | ------------ |
| Bundle size | 0 KB        | ~1 KB        | ~11 KB        | ~13 KB       |
| Boilerplate | Ít          | **Rất ít**   | Trung bình    | Ít           |
| DevTools    | ❌          | ✅           | ✅            | ✅           |
| Middleware  | ❌          | ✅           | ✅            | N/A          |
| Best for    | Theme, Auth | Client state | Complex state | Server state |

---

## 1. Context + useReducer (Built-in)

Xem lại [2.5 Context API](./2.5-context-api.md) cho cơ bản. Dưới đây là pattern nâng cao.

### 1.1 Full Example — Todo App với Context + useReducer

```tsx
// context/TodoContext.tsx
import React, { createContext, useContext, useReducer, ReactNode } from "react";

// ===== Types =====
interface Todo {
  id: string;
  text: string;
  completed: boolean;
  createdAt: Date;
}

interface TodoState {
  todos: Todo[];
  filter: "all" | "active" | "completed";
}

// ===== Actions (discriminated union) =====
type TodoAction =
  | { type: "ADD_TODO"; payload: string }
  | { type: "TOGGLE_TODO"; payload: string }
  | { type: "DELETE_TODO"; payload: string }
  | { type: "SET_FILTER"; payload: TodoState["filter"] }
  | { type: "CLEAR_COMPLETED" };

// ===== Reducer =====
const todoReducer = (state: TodoState, action: TodoAction): TodoState => {
  switch (action.type) {
    case "ADD_TODO":
      return {
        ...state,
        todos: [
          ...state.todos,
          {
            id: Date.now().toString(),
            text: action.payload,
            completed: false,
            createdAt: new Date(),
          },
        ],
      };
    case "TOGGLE_TODO":
      return {
        ...state,
        todos: state.todos.map((todo) =>
          todo.id === action.payload
            ? { ...todo, completed: !todo.completed }
            : todo,
        ),
      };
    case "DELETE_TODO":
      return {
        ...state,
        todos: state.todos.filter((todo) => todo.id !== action.payload),
      };
    case "SET_FILTER":
      return { ...state, filter: action.payload };
    case "CLEAR_COMPLETED":
      return {
        ...state,
        todos: state.todos.filter((todo) => !todo.completed),
      };
    default:
      return state;
  }
};

// ===== Context =====
interface TodoContextType {
  state: TodoState;
  dispatch: React.Dispatch<TodoAction>;
  // Computed / selectors
  filteredTodos: Todo[];
  activeCount: number;
}

const TodoContext = createContext<TodoContextType | undefined>(undefined);

// ===== Provider =====
const initialState: TodoState = {
  todos: [],
  filter: "all",
};

export const TodoProvider = ({ children }: { children: ReactNode }) => {
  const [state, dispatch] = useReducer(todoReducer, initialState);

  // Computed values
  const filteredTodos = state.todos.filter((todo) => {
    if (state.filter === "active") return !todo.completed;
    if (state.filter === "completed") return todo.completed;
    return true;
  });

  const activeCount = state.todos.filter((t) => !t.completed).length;

  return (
    <TodoContext.Provider
      value={{ state, dispatch, filteredTodos, activeCount }}
    >
      {children}
    </TodoContext.Provider>
  );
};

// ===== Custom Hook =====
export const useTodos = () => {
  const context = useContext(TodoContext);
  if (!context) {
    throw new Error("useTodos must be used within TodoProvider");
  }
  return context;
};
```

```tsx
// screens/TodoScreen.tsx
import React, { useState } from "react";
import {
  View,
  Text,
  TextInput,
  TouchableOpacity,
  FlatList,
  StyleSheet,
} from "react-native";
import { useTodos } from "../context/TodoContext";

const TodoScreen = () => {
  const { state, dispatch, filteredTodos, activeCount } = useTodos();
  const [input, setInput] = useState("");

  const handleAdd = () => {
    if (input.trim()) {
      dispatch({ type: "ADD_TODO", payload: input.trim() });
      setInput("");
    }
  };

  return (
    <View style={styles.container}>
      {/* Input */}
      <View style={styles.inputRow}>
        <TextInput
          style={styles.input}
          placeholder="What needs to be done?"
          value={input}
          onChangeText={setInput}
          onSubmitEditing={handleAdd}
        />
        <TouchableOpacity style={styles.addBtn} onPress={handleAdd}>
          <Text style={styles.addBtnText}>+</Text>
        </TouchableOpacity>
      </View>

      {/* Filters */}
      <View style={styles.filterRow}>
        {(["all", "active", "completed"] as const).map((f) => (
          <TouchableOpacity
            key={f}
            style={[
              styles.filterBtn,
              state.filter === f && styles.filterActive,
            ]}
            onPress={() => dispatch({ type: "SET_FILTER", payload: f })}
          >
            <Text
              style={[
                styles.filterText,
                state.filter === f && { color: "#fff" },
              ]}
            >
              {f}
            </Text>
          </TouchableOpacity>
        ))}
      </View>

      {/* List */}
      <FlatList
        data={filteredTodos}
        keyExtractor={(item) => item.id}
        renderItem={({ item }) => (
          <View style={styles.todoItem}>
            <TouchableOpacity
              style={styles.todoContent}
              onPress={() =>
                dispatch({ type: "TOGGLE_TODO", payload: item.id })
              }
            >
              <Text style={styles.checkbox}>
                {item.completed ? "✅" : "⬜"}
              </Text>
              <Text
                style={[
                  styles.todoText,
                  item.completed && styles.todoCompleted,
                ]}
              >
                {item.text}
              </Text>
            </TouchableOpacity>
            <TouchableOpacity
              onPress={() =>
                dispatch({ type: "DELETE_TODO", payload: item.id })
              }
            >
              <Text style={styles.deleteBtn}>🗑️</Text>
            </TouchableOpacity>
          </View>
        )}
        ListEmptyComponent={<Text style={styles.empty}>No todos yet!</Text>}
      />

      {/* Footer */}
      <View style={styles.footer}>
        <Text style={styles.footerText}>{activeCount} items left</Text>
        <TouchableOpacity onPress={() => dispatch({ type: "CLEAR_COMPLETED" })}>
          <Text style={styles.clearBtn}>Clear completed</Text>
        </TouchableOpacity>
      </View>
    </View>
  );
};

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: "#fff", padding: 16 },
  inputRow: { flexDirection: "row", gap: 8, marginBottom: 12 },
  input: {
    flex: 1,
    borderWidth: 1,
    borderColor: "#ddd",
    borderRadius: 8,
    padding: 12,
    fontSize: 16,
  },
  addBtn: {
    width: 48,
    backgroundColor: "#007AFF",
    borderRadius: 8,
    justifyContent: "center",
    alignItems: "center",
  },
  addBtnText: { color: "#fff", fontSize: 24 },
  filterRow: { flexDirection: "row", gap: 8, marginBottom: 12 },
  filterBtn: {
    paddingHorizontal: 16,
    paddingVertical: 8,
    borderRadius: 20,
    borderWidth: 1,
    borderColor: "#ddd",
  },
  filterActive: { backgroundColor: "#007AFF", borderColor: "#007AFF" },
  filterText: { fontSize: 14, color: "#666", textTransform: "capitalize" },
  todoItem: {
    flexDirection: "row",
    alignItems: "center",
    paddingVertical: 12,
    borderBottomWidth: 1,
    borderBottomColor: "#f0f0f0",
  },
  todoContent: { flex: 1, flexDirection: "row", alignItems: "center", gap: 12 },
  checkbox: { fontSize: 20 },
  todoText: { fontSize: 16, flex: 1 },
  todoCompleted: { textDecorationLine: "line-through", color: "#999" },
  deleteBtn: { fontSize: 18, padding: 4 },
  empty: { textAlign: "center", color: "#999", marginTop: 40, fontSize: 16 },
  footer: {
    flexDirection: "row",
    justifyContent: "space-between",
    paddingTop: 12,
    borderTopWidth: 1,
    borderTopColor: "#f0f0f0",
  },
  footerText: { color: "#666" },
  clearBtn: { color: "#dc3545" },
});

export default TodoScreen;
```

---

## 2. Zustand (Recommended cho hầu hết apps)

### 2.1 Cài Đặt

```bash
npx expo install zustand
```

### 2.2 Tạo Store

```tsx
// stores/useCounterStore.ts
import { create } from "zustand";

interface CounterState {
  count: number;
  increment: () => void;
  decrement: () => void;
  reset: () => void;
  incrementBy: (amount: number) => void;
}

// ✅ Zustand store — simple, no Provider needed!
export const useCounterStore = create<CounterState>((set) => ({
  count: 0,
  increment: () => set((state) => ({ count: state.count + 1 })),
  decrement: () => set((state) => ({ count: state.count - 1 })),
  reset: () => set({ count: 0 }),
  incrementBy: (amount) => set((state) => ({ count: state.count + amount })),
}));
```

```tsx
// Sử dụng — Không cần Provider!
import React from "react";
import { View, Text, TouchableOpacity, StyleSheet } from "react-native";
import { useCounterStore } from "../stores/useCounterStore";

const CounterScreen = () => {
  // ✅ Chỉ subscribe field cần thiết → tránh re-render thừa
  const count = useCounterStore((state) => state.count);
  const { increment, decrement, reset } = useCounterStore();

  return (
    <View style={zStyles.container}>
      <Text style={zStyles.count}>{count}</Text>
      <View style={zStyles.row}>
        <TouchableOpacity style={zStyles.btn} onPress={decrement}>
          <Text style={zStyles.btnText}>−</Text>
        </TouchableOpacity>
        <TouchableOpacity
          style={[zStyles.btn, { backgroundColor: "#999" }]}
          onPress={reset}
        >
          <Text style={zStyles.btnText}>Reset</Text>
        </TouchableOpacity>
        <TouchableOpacity style={zStyles.btn} onPress={increment}>
          <Text style={zStyles.btnText}>+</Text>
        </TouchableOpacity>
      </View>
    </View>
  );
};

const zStyles = StyleSheet.create({
  container: { flex: 1, justifyContent: "center", alignItems: "center" },
  count: { fontSize: 64, fontWeight: "bold", marginBottom: 24 },
  row: { flexDirection: "row", gap: 16 },
  btn: {
    width: 64,
    height: 64,
    backgroundColor: "#007AFF",
    borderRadius: 32,
    justifyContent: "center",
    alignItems: "center",
  },
  btnText: { color: "#fff", fontSize: 24, fontWeight: "bold" },
});
```

### 2.3 Real-World Store — Auth

```tsx
// stores/useAuthStore.ts
import { create } from "zustand";
// import AsyncStorage from '@react-native-async-storage/async-storage';

interface User {
  id: string;
  name: string;
  email: string;
  avatar?: string;
}

interface AuthState {
  user: User | null;
  token: string | null;
  isLoading: boolean;
  isAuthenticated: boolean;

  // Actions
  login: (email: string, password: string) => Promise<void>;
  logout: () => Promise<void>;
  loadStoredAuth: () => Promise<void>;
  updateProfile: (data: Partial<User>) => void;
}

export const useAuthStore = create<AuthState>((set, get) => ({
  user: null,
  token: null,
  isLoading: false,
  isAuthenticated: false,

  login: async (email, password) => {
    set({ isLoading: true });
    try {
      // Giả lập API call
      const response = await fetch("https://reqres.in/api/login", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ email, password }),
      });

      if (!response.ok) throw new Error("Login failed");

      const data = await response.json();

      // Lưu vào AsyncStorage (persistent)
      // await AsyncStorage.setItem('token', data.token);

      set({
        token: data.token,
        user: { id: "1", name: "User", email },
        isAuthenticated: true,
        isLoading: false,
      });
    } catch (error) {
      set({ isLoading: false });
      throw error;
    }
  },

  logout: async () => {
    // await AsyncStorage.removeItem('token');
    set({
      user: null,
      token: null,
      isAuthenticated: false,
    });
  },

  loadStoredAuth: async () => {
    set({ isLoading: true });
    try {
      // const token = await AsyncStorage.getItem('token');
      const token = null; // placeholder
      if (token) {
        // Validate token / fetch user profile
        set({ token, isAuthenticated: true });
      }
    } finally {
      set({ isLoading: false });
    }
  },

  updateProfile: (data) => {
    const currentUser = get().user;
    if (currentUser) {
      set({ user: { ...currentUser, ...data } });
    }
  },
}));
```

### 2.4 Zustand — Persist Middleware

```tsx
// stores/useSettingsStore.ts
import { create } from "zustand";
import { persist, createJSONStorage } from "zustand/middleware";
import AsyncStorage from "@react-native-async-storage/async-storage";

interface SettingsState {
  theme: "light" | "dark" | "system";
  language: "vi" | "en";
  notificationsEnabled: boolean;

  setTheme: (theme: SettingsState["theme"]) => void;
  setLanguage: (lang: SettingsState["language"]) => void;
  toggleNotifications: () => void;
}

export const useSettingsStore = create<SettingsState>()(
  persist(
    (set) => ({
      theme: "system",
      language: "vi",
      notificationsEnabled: true,

      setTheme: (theme) => set({ theme }),
      setLanguage: (language) => set({ language }),
      toggleNotifications: () =>
        set((state) => ({ notificationsEnabled: !state.notificationsEnabled })),
    }),
    {
      name: "settings-storage", // Key trong AsyncStorage
      storage: createJSONStorage(() => AsyncStorage),
    },
  ),
);
```

### 2.5 Zustand — Slices Pattern (Chia nhỏ store lớn)

```tsx
// stores/slices/userSlice.ts
import { StateCreator } from "zustand";

export interface UserSlice {
  user: { name: string; email: string } | null;
  setUser: (user: UserSlice["user"]) => void;
  clearUser: () => void;
}

export const createUserSlice: StateCreator<UserSlice> = (set) => ({
  user: null,
  setUser: (user) => set({ user }),
  clearUser: () => set({ user: null }),
});

// stores/slices/cartSlice.ts
export interface CartSlice {
  items: { id: string; name: string; qty: number }[];
  addItem: (item: { id: string; name: string }) => void;
  clearCart: () => void;
  totalItems: () => number;
}

export const createCartSlice: StateCreator<
  CartSlice & UserSlice,
  [],
  [],
  CartSlice
> = (set, get) => ({
  items: [],
  addItem: (item) =>
    set((state) => {
      const existing = state.items.find((i) => i.id === item.id);
      if (existing) {
        return {
          items: state.items.map((i) =>
            i.id === item.id ? { ...i, qty: i.qty + 1 } : i,
          ),
        };
      }
      return { items: [...state.items, { ...item, qty: 1 }] };
    }),
  clearCart: () => set({ items: [] }),
  totalItems: () => get().items.reduce((sum, i) => sum + i.qty, 0),
});

// stores/useAppStore.ts — Combine slices
import { create } from "zustand";
// import { createUserSlice, UserSlice } from './slices/userSlice';
// import { createCartSlice, CartSlice } from './slices/cartSlice';

// export const useAppStore = create<UserSlice & CartSlice>()((...args) => ({
//   ...createUserSlice(...args),
//   ...createCartSlice(...args),
// }));
```

---

## 3. Redux Toolkit (Cho apps phức tạp)

### 3.1 Cài Đặt

```bash
npx expo install @reduxjs/toolkit react-redux
```

### 3.2 Slice

```tsx
// store/slices/todosSlice.ts
import { createSlice, PayloadAction } from "@reduxjs/toolkit";

interface Todo {
  id: string;
  text: string;
  completed: boolean;
}

interface TodosState {
  items: Todo[];
  filter: "all" | "active" | "completed";
}

const initialState: TodosState = {
  items: [],
  filter: "all",
};

const todosSlice = createSlice({
  name: "todos",
  initialState,
  reducers: {
    addTodo: (state, action: PayloadAction<string>) => {
      // ✅ RTK dùng Immer — có thể mutate trực tiếp!
      state.items.push({
        id: Date.now().toString(),
        text: action.payload,
        completed: false,
      });
    },
    toggleTodo: (state, action: PayloadAction<string>) => {
      const todo = state.items.find((t) => t.id === action.payload);
      if (todo) {
        todo.completed = !todo.completed;
      }
    },
    deleteTodo: (state, action: PayloadAction<string>) => {
      state.items = state.items.filter((t) => t.id !== action.payload);
    },
    setFilter: (state, action: PayloadAction<TodosState["filter"]>) => {
      state.filter = action.payload;
    },
  },
});

export const { addTodo, toggleTodo, deleteTodo, setFilter } =
  todosSlice.actions;
export default todosSlice.reducer;
```

### 3.3 Store

```tsx
// store/index.ts
import { configureStore } from "@reduxjs/toolkit";
import todosReducer from "./slices/todosSlice";

export const store = configureStore({
  reducer: {
    todos: todosReducer,
    // auth: authReducer,
    // cart: cartReducer,
  },
});

// Types
export type RootState = ReturnType<typeof store.getState>;
export type AppDispatch = typeof store.dispatch;

// Typed hooks
import { useDispatch, useSelector, TypedUseSelectorHook } from "react-redux";
export const useAppDispatch = () => useDispatch<AppDispatch>();
export const useAppSelector: TypedUseSelectorHook<RootState> = useSelector;
```

### 3.4 Sử Dụng

```tsx
// App.tsx — Wrap Provider
import { Provider } from "react-redux";
import { store } from "./store";

export default function App() {
  return <Provider store={store}>{/* App content */}</Provider>;
}

// screens/TodoScreen.tsx
import React, { useState } from "react";
import {
  View,
  Text,
  TextInput,
  TouchableOpacity,
  FlatList,
} from "react-native";
import { useAppDispatch, useAppSelector } from "../store";
import { addTodo, toggleTodo, deleteTodo } from "../store/slices/todosSlice";

const ReduxTodoScreen = () => {
  const [input, setInput] = useState("");
  const dispatch = useAppDispatch();
  const todos = useAppSelector((state) => state.todos.items);

  return (
    <View style={{ flex: 1, padding: 16 }}>
      <TextInput
        value={input}
        onChangeText={setInput}
        placeholder="Add todo..."
        onSubmitEditing={() => {
          if (input.trim()) {
            dispatch(addTodo(input.trim()));
            setInput("");
          }
        }}
        style={{
          borderWidth: 1,
          borderColor: "#ddd",
          borderRadius: 8,
          padding: 12,
          marginBottom: 12,
        }}
      />
      <FlatList
        data={todos}
        keyExtractor={(item) => item.id}
        renderItem={({ item }) => (
          <TouchableOpacity
            onPress={() => dispatch(toggleTodo(item.id))}
            onLongPress={() => dispatch(deleteTodo(item.id))}
            style={{
              padding: 12,
              borderBottomWidth: 1,
              borderBottomColor: "#f0f0f0",
            }}
          >
            <Text
              style={{
                textDecorationLine: item.completed ? "line-through" : "none",
              }}
            >
              {item.completed ? "✅" : "⬜"} {item.text}
            </Text>
          </TouchableOpacity>
        )}
      />
    </View>
  );
};
```

---

## 4. TanStack Query (Server State)

### 4.1 Cài Đặt

```bash
npx expo install @tanstack/react-query
```

### 4.2 Setup & Sử Dụng

```tsx
// App.tsx
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";

const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 5 * 60 * 1000, // 5 phút
      retry: 2,
    },
  },
});

export default function App() {
  return (
    <QueryClientProvider client={queryClient}>{/* App */}</QueryClientProvider>
  );
}
```

```tsx
// hooks/useUsers.ts
import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";

interface User {
  id: number;
  first_name: string;
  last_name: string;
  email: string;
  avatar: string;
}

// Fetch function
const fetchUsers = async (
  page: number,
): Promise<{ data: User[]; total_pages: number }> => {
  const res = await fetch(`https://reqres.in/api/users?page=${page}`);
  if (!res.ok) throw new Error("Failed to fetch");
  return res.json();
};

// ✅ useQuery — GET request with caching
export const useUsers = (page: number = 1) => {
  return useQuery({
    queryKey: ["users", page], // Cache key
    queryFn: () => fetchUsers(page),
  });
};

// ✅ useMutation — POST/PUT/DELETE
export const useCreateUser = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async (user: { name: string; job: string }) => {
      const res = await fetch("https://reqres.in/api/users", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify(user),
      });
      return res.json();
    },
    onSuccess: () => {
      // Invalidate cache → refetch users list
      queryClient.invalidateQueries({ queryKey: ["users"] });
    },
  });
};
```

```tsx
// screens/UsersScreen.tsx
import React from "react";
import {
  View,
  Text,
  FlatList,
  ActivityIndicator,
  TouchableOpacity,
} from "react-native";
import { useUsers } from "../hooks/useUsers";

const UsersScreen = () => {
  const { data, isLoading, isError, error, refetch, isFetching } = useUsers(1);

  if (isLoading) return <ActivityIndicator size="large" style={{ flex: 1 }} />;
  if (isError) return <Text>Error: {error.message}</Text>;

  return (
    <FlatList
      data={data?.data}
      keyExtractor={(item) => item.id.toString()}
      renderItem={({ item }) => (
        <View
          style={{
            padding: 16,
            flexDirection: "row",
            gap: 12,
            alignItems: "center",
          }}
        >
          <Text style={{ fontSize: 16 }}>
            {item.first_name} {item.last_name}
          </Text>
        </View>
      )}
      refreshing={isFetching}
      onRefresh={refetch}
    />
  );
};
```

---

## 5. Quyết Định Chọn Gì?

```
Bạn cần quản lý gì?
│
├── Server state (API data)?
│   └── ✅ TanStack Query
│
├── Form state?
│   └── ✅ React Hook Form (xem 3.8)
│
├── Client state (UI state, preferences)?
│   │
│   ├── Chỉ 1-2 components?
│   │   └── ✅ useState
│   │
│   ├── Chia sẻ gần (2-3 cấp)?
│   │   └── ✅ Context + useReducer
│   │
│   └── Chia sẻ toàn app / phức tạp?
│       ├── App nhỏ-trung bình → ✅ Zustand
│       └── App lớn / team lớn → ✅ Redux Toolkit
│
└── Cần cả hai? → TanStack Query + Zustand
```

---

## 🏋️ Bài Tập Thực Hành

### Bài 1: Todo App với Context + useReducer

Xem code mẫu phần 1 — implement đầy đủ CRUD + filter.

### Bài 2: Zustand Store

Tạo app Shopping Cart với Zustand:

- Product list screen
- Add to cart
- Cart screen (update qty, remove, total price)
- Persist cart với AsyncStorage

### Bài 3: TanStack Query

Tạo app hiển thị users từ API với:

- Auto caching
- Pull to refresh
- Pagination
- Create user mutation

---

## 📚 Tài Liệu Tham Khảo

| Nguồn            | Link                                                            |
| ---------------- | --------------------------------------------------------------- |
| Zustand          | https://docs.pmnd.rs/zustand/getting-started/introduction       |
| Redux Toolkit    | https://redux-toolkit.js.org/introduction/getting-started       |
| TanStack Query   | https://tanstack.com/query/latest/docs/framework/react/overview |
| React useReducer | https://react.dev/reference/react/useReducer                    |
| Zustand Persist  | https://docs.pmnd.rs/zustand/integrations/persisting-store-data |

---

## ✅ Checklist Tự Đánh Giá

### Context + useReducer

- [ ] `useReducer` với typed actions
- [ ] Context Provider pattern
- [ ] Custom hook (`useTodos`)
- [ ] Computed values trong Provider

### Zustand

- [ ] `create` store
- [ ] Selector pattern (tránh re-render)
- [ ] Persist middleware
- [ ] Slices pattern

### Redux Toolkit

- [ ] `createSlice` (reducers + actions)
- [ ] `configureStore`
- [ ] `useSelector` / `useDispatch`
- [ ] Typed hooks

### TanStack Query

- [ ] `useQuery` (GET + caching)
- [ ] `useMutation` (POST/PUT/DELETE)
- [ ] `invalidateQueries`
- [ ] Loading/error states

> **Trước đó**: [← 3.6 API Integration](./3.6-api-integration.md) | **Tiếp theo**: [3.8 Form Handling →](./3.8-form-handling.md)
